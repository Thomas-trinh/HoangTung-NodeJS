<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Category Index</title>
    <style>
        table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
        }

        td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
        }

        tr:nth-child(even) {
        background-color: #dddddd;
        }
    </style>
</head>
<body>
<!-- Navbar -->
<div class="nav">
    <div class="navbar">
        <a href="/">Index</a>
        <a href="/viewCategory">Category</a>
    </div>
</div>
<!-- Page content -->
<div class="content">
    <h1>View categories</h1>
    <span>
        <p style="top bottom:5%; font-size:large; color:red">{{msg}}</p>
        <br>
    </span>
    <a href="/createCategory">Add</a>
    <br>
    <table>
        {{#each categories}}
        <tr>
            <td>ID</td>
            <td><strong>Name</strong></td>
            <td><strong>Description</strong></td>
            <td><strong>List of Products</strong></td>
            <td><strong>Actions</strong></td>
        </tr>
        <tr>
            <td>{{id}}</td>
            <td>{{name}}</td>
            <td>{{description}}</td>
            <td>
                {{#each products}}
                    <p>{{name}}</p>
                {{/each}}
            </td>
            <td>
                <a href="/deleteCategory?id={{_id}}">Delete</a>
                <a href="/editCategory?id={{_id}}">Edit</a>
            </td>
        </tr>
        {{/each}}
    </table>
</div>
</body>
</html>